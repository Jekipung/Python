선택정렬 완성본
a=[]
stack=0
N=int(input())
for i in range(N):
    a.append(int(input()))
print(a)
b=0
for m in range(len(a)):
    b = m
    for i in range(m,len(a)):
        if a[b] > a[i]:
            b=i
    a[m] , a[b] = a[b] , a[m]
    print(a)



class란? /틀이다. -틀- 한가지 틀을 만들어서 여러개의 객체를 찍어낼수 있기 때문이다.

class를 선언하는 방법.
calss 클래스이름(상속받을 부모클래스의 이름):
상속받는 클래스가 자식 클래스
상속 당하는 클래스가 부모 클래스

init /생성될때 한번 실행하는 클래스 내장함수
사용방법 /def init (self):
import random
class lol():
    def init (self):
        self.gg = random.randrange(11)
a=lol()
print(a.gg)
self를 써야하는 이유는 그 자리에 앞으로의 객체이름이 들어간다.
**부가설명 필요**

실행할경우:0~10의 랜덤한 수가 나온다

과자틀-클래스
과자-객체(dbject)

클래스로 만들어진 변수를 객체 또는 인스턴스 라고도 부르는데
이 둘의 차이점은 calss a로 만들어진 변수 b가 있다고 가정할때
"b는 인스턴스" 보다 "b는 객체" 라는 말이 더 알맞고,
"a는 b의 객체" 보다 "a는 b의 인스턴스"라는 말이 더욱 알맞다.
