```
// # 은 전처리과정
```

```
// 넓이, 둘레 구하는 코드
/*#include <stdio.h>

int main() {
	double h, w, area, perimeter;

	printf("높이를 설정히세요 : ");
	scanf("%lf", &h);

	printf("너비를 입력하세요: ");
	scanf("%lf", &w);

	area = w * h; // 면적
	perimeter = 2 * (w + h); // 둘레

	printf("면적: %lf\n", area);
	printf("둘레: %lf\n", perimeter);

	return 0;
}*/
```

```
// 함수의 크기 보는 코드

/*
#include <stdio.h>

int main() {
	int x;

	printf("변수 x의 크키 : %d\n", sizeof(x));
	printf("char 형의 크기: %d\n", sizeof(char));
	printf("int형의 크기: %d\n", sizeof(int));
	printf("short 형의 크기: %d\n", sizeof(short));
	printf("long 형의 크기: %d\n", sizeof(long));
	printf("float 형의 크기: %d\n", sizeof(float));
	printf("double 형의 크기: %d", sizeof(double));

}
*/
```

```
/*2의 보수 프로그램*/
/*
#include <stdio.h>

int main() {

	int x = 3;
	int y = -3;

	printf("x = %08x\n", x);  // 8자리의 16진수로 출력
	printf("y = %08x\n", y);   // 8자리의 16진수로 출력
	printf("x + y = %08x\n", x + y);  // 8자리의 16 진수로 출력


	return 0;
}
*/
```
